source "https://rubygems.org"

# Use GitHub Pages’ ruby gems (optional but mirrors GH Pages env)
gem "github-pages", group: :jekyll_plugins #

gem "jekyll-include-cache", group: :jekyll_plugins # Add group to match github-pages gem

gem "jekyll-remote-theme", group: :jekyll_plugins # Add group to match github-pages gem
