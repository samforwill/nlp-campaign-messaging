source "https://rubygems.org"

# Use GitHub Pages’ ruby gems (optional but mirrors GH Pages env)
group :jekyll_plugins do # Add group: :jekyll_plugins to your gemfile
  gem "github-pages"
  gem "jekyll-include-cache"
  gem "jekyll-remote-theme" #
end #
